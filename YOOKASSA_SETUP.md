# 💳 Настройка ЮKassa для приема платежей

Это руководство поможет вам настроить интеграцию с платежной системой ЮKassa для приема оплаты за курсы.

---

## 📋 Содержание

1. [Что такое ЮKassa](#что-такое-юkassa)
2. [Регистрация в ЮKassa](#регистрация-в-юkassa)
3. [Получение учетных данных](#получение-учетных-данных)
4. [Настройка проекта](#настройка-проекта)
5. [Настройка Webhook](#настройка-webhook)
6. [Тестирование платежей](#тестирование-платежей)
7. [Переход в production](#переход-в-production)
8. [FAQ и решение проблем](#faq-и-решение-проблем)

---

## 🎯 Что такое ЮKassa

**ЮKassa** (ранее Яндекс.Касса) — это российская платежная система от Yandex, которая:

- ✅ Принимает платежи картами, через СБП, электронные кошельки
- ✅ Автоматически отправляет чеки в налоговую (54-ФЗ)
- ✅ Работает с российскими юридическими лицами и ИП
- ✅ Имеет официальный Python SDK
- ✅ Предоставляет тестовую среду для разработки

**Комиссия:** от 2.8% до 4.5% в зависимости от оборота и типа платежей.

---

## 📝 Регистрация в ЮKassa

### Шаг 1: Создание аккаунта

1. Перейдите на сайт [yookassa.ru](https://yookassa.ru)
2. Нажмите "Подключиться"
3. Заполните форму регистрации:
   - ИНН организации
   - Email
   - Телефон
4. Подтвердите email

### Шаг 2: Заполнение данных компании

После регистрации вам нужно предоставить:
- Скан устава или учредительных документов
- Скан паспорта директора
- Описание бизнеса (что продаете, аудитория)
- Адрес сайта

**Модерация занимает 1-3 рабочих дня.**

---

## 🔑 Получение учетных данных

После одобрения заявки:

### 1. Войдите в личный кабинет ЮKassa

https://yookassa.ru/my

### 2. Перейдите в раздел "Настройки" → "Интеграция"

### 3. Найдите учетные данные:

- **Shop ID** — идентификатор вашего магазина (например: `123456`)
- **Secret Key** — секретный ключ для API (например: `live_abc123...`)

**ВАЖНО:** Secret Key показывается только один раз! Сохраните его в надежном месте.

### 4. Если потеряли Secret Key

- Нажмите "Сгенерировать новый ключ"
- Старый ключ перестанет работать!

---

## ⚙️ Настройка проекта

### 1. Добавьте учетные данные в `.env`

```env
# ЮKassa настройки
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=live_abc123def456ghi789jkl
YOOKASSA_RETURN_URL=https://neuro-magic.ru/payment/success
```

**Для тестирования используйте тестовые ключи:**
```env
YOOKASSA_SHOP_ID=123456
YOOKASSA_SECRET_KEY=test_abc123def456ghi789jkl
```

### 2. Установите зависимости

```bash
pip install -r requirements.txt
```

Это установит:
- `yookassa==3.8.0` — официальный Python SDK

### 3. Пересоздайте базу данных

После добавления модели `Payment` нужно пересоздать таблицы:

```bash
# Остановите приложение
docker-compose down

# Удалите volumes (если используете Docker)
docker-compose down -v

# Запустите снова
docker-compose up -d --build
```

Или локально:
```bash
# Удалите старую БД
rm neuromagic.db

# Запустите приложение — таблицы создадутся автоматически
quart --app main:app run
```

---

## 🔔 Настройка Webhook

Webhook — это URL, на который ЮKassa будет отправлять уведомления о статусе платежей.

### 1. Убедитесь что ваш сайт доступен по HTTPS

⚠️ **ВАЖНО:** ЮKassa отправляет webhook только на HTTPS!

Локальный http://localhost **НЕ БУДЕТ РАБОТАТЬ**.

### 2. Настройте webhook URL в личном кабинете ЮKassa

1. Откройте [Настройки → Уведомления](https://yookassa.ru/my/merchant/integration/http-notifications)
2. Включите HTTP-уведомления
3. Укажите URL: `https://neuro-magic.ru/payments/webhook`
4. Выберите события:
   - ✅ `payment.succeeded` — успешный платеж
   - ✅ `payment.waiting_for_capture` — ожидание подтверждения
   - ✅ `payment.canceled` — отмена платежа
5. Сохраните настройки

### 3. Для тестирования на локальной машине

Используйте **ngrok** или **localtunnel** для создания туннеля:

```bash
# Установите ngrok
npm install -g ngrok

# Запустите туннель
ngrok http 8000

# Вы получите URL вида: https://abc123.ngrok.io
# Используйте его в настройках webhook: https://abc123.ngrok.io/payments/webhook
```

---

## 🧪 Тестирование платежей

### Тестовые карты

ЮKassa предоставляет тестовые карты для проверки:

| Карта | Результат |
|-------|-----------|
| `5555 5555 5555 4444` | Успешная оплата |
| `5555 5555 5555 5599` | Отклонение банком |

**Любой CVV (3 цифры)** и **любая дата в будущем**.

### Проверка workflow

1. **Создание платежа:**
   - Войдите на сайт
   - Откройте страницу курса
   - Нажмите "Купить курс"
   - Проверьте что открылась форма ЮKassa

2. **Оплата:**
   - Введите тестовую карту `5555 5555 5555 4444`
   - Нажмите "Оплатить"
   - Вас перенаправит на `/payment/success`

3. **Проверка webhook:**
   - Откройте логи: `docker-compose logs -f app`
   - Найдите строку: `Webhook processed successfully for payment #123`

4. **Проверка email:**
   - Проверьте почту пользователя
   - Должно прийти письмо с подтверждением покупки

5. **Проверка БД:**
   ```bash
   docker-compose exec postgres psql -U neuromagic_user -d neuromagic
   SELECT * FROM payments ORDER BY created_at DESC LIMIT 5;
   ```

---

## 🚀 Переход в production

### 1. Замените тестовые ключи на боевые

В `.env` на сервере:
```env
YOOKASSA_SHOP_ID=ваш_реальный_shop_id
YOOKASSA_SECRET_KEY=live_ваш_реальный_секретный_ключ
YOOKASSA_RETURN_URL=https://neuro-magic.ru/payment/success
```

### 2. Настройте правильный webhook URL

В личном кабинете ЮKassa:
- URL: `https://neuro-magic.ru/payments/webhook`
- Убедитесь что SSL сертификат валидный!

### 3. Добавьте мониторинг платежей

Создайте раздел в админ-панели для просмотра платежей:
```python
# TODO: admin route для просмотра payments
@admin_bp.route("/admin/payments")
@login_required
async def admin_payments():
    # Показать все платежи из БД
    pass
```

### 4. Настройте email уведомления

Убедитесь что SMTP настроен в production:
```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=ваш-реальный-email@yandex.ru
SMTP_PASSWORD=пароль-приложения
```

### 5. Проверьте налоговую отчетность

ЮKassa **автоматически** отправляет чеки в ФНС.

Проверить чеки можно в личном кабинете ЮKassa → Отчеты → Чеки.

---

## ❓ FAQ и решение проблем

### 1. Ошибка "Invalid credentials"

**Причина:** Неверный Shop ID или Secret Key.

**Решение:**
- Проверьте `.env` на сервере
- Убедитесь что нет лишних пробелов
- В production используйте `live_*` ключ, не `test_*`

### 2. Webhook не приходят

**Причина:** Сайт недоступен по HTTPS или неверный URL.

**Решение:**
- Проверьте что сайт работает: `curl https://neuro-magic.ru/payments/webhook`
- Проверьте логи nginx: `docker-compose logs nginx`
- Убедитесь что SSL сертификат валидный
- Проверьте настройки в личном кабинете ЮKassa

### 3. Платеж создается, но не завершается

**Причина:** Проблема с обработкой webhook.

**Решение:**
- Откройте логи: `docker-compose logs -f app`
- Найдите строку `Processing webhook: event=payment.succeeded`
- Проверьте что нет ошибок в `process_webhook()`

### 4. Email не приходит после оплаты

**Причина:** SMTP не настроен или ошибка отправки.

**Решение:**
- Проверьте логи: `Failed to send purchase email`
- Проверьте `.env`: SMTP_USER, SMTP_PASSWORD
- Проверьте что SMTP_USER совпадает с SMTP_FROM_EMAIL

### 5. Пользователь видит ошибку 500

**Причина:** Не установлен пакет `yookassa`.

**Решение:**
```bash
pip install yookassa==3.8.0
# Или
docker-compose up -d --build
```

---

## 📊 Архитектура интеграции

```
Пользователь нажимает "Купить курс"
         ↓
POST /payments/create (создание платежа в ЮKassa)
         ↓
Сохранение Payment в БД (status=pending)
         ↓
Redirect на confirmation_url (форма ЮKassa)
         ↓
Пользователь вводит данные карты
         ↓
ЮKassa обрабатывает платеж
         ↓
POST /payments/webhook (уведомление от ЮKassa)
         ↓
Обновление Payment в БД (status=succeeded)
         ↓
Отправка email пользователю
         ↓
TODO: Активация доступа к курсу (UserCourse)
```

---

## 🛡️ Безопасность

### Реализовано:
- ✅ Платежи через официальный SDK ЮKassa
- ✅ HTTPS для webhook
- ✅ Хранение Secret Key в переменных окружения
- ✅ Валидация данных через Pydantic

### TODO (для production):
- [ ] Проверка IP адреса webhook (только от ЮKassa)
- [ ] Rate limiting для API
- [ ] Логирование всех платежей
- [ ] Мониторинг ошибок (Sentry)

---

## 📞 Поддержка ЮKassa

- **Телефон:** 8 800 234-24-80 (бесплатно по России)
- **Email:** help@yookassa.ru
- **Документация:** https://yookassa.ru/developers
- **Личный кабинет:** https://yookassa.ru/my

---

**Последнее обновление:** 2025-11-20

**Готово к использованию!** 🎉
