# 🚀 Инструкция по деплою на сервер

## 📋 Данные сервера

```
IPv4: 138.124.15.60
Логин: root
Пароль: PFIwOaXLAo08Wz
```

## 🔧 Шаг 1: Подключение к серверу

```bash
ssh root@138.124.15.60
# Введите пароль: PFIwOaXLAo08Wz
```

## 📦 Шаг 2: Первоначальная настройка

### 2.1. Установка Docker и Docker Compose

```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
rm get-docker.sh

# Устанавливаем Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# Проверяем установку
docker --version
docker-compose --version
```

### 2.2. Установка Git

```bash
apt install git -y
git --version
```

## 🏗️ Шаг 3: Клонирование проекта

```bash
# Создаём директорию для проекта
mkdir -p /opt/neuromagic
cd /opt/neuromagic

# Клонируем репозиторий
git clone https://github.com/Starlight19973/magic_ai.git .

# Проверяем что всё скопировалось
ls -la
```

## 🔑 Шаг 4: Создание .env файла

```bash
# Создаём .env файл на сервере
cat > .env << 'EOF'
SECRET_KEY=prod_secret_key_2024_neuromagic_ultra_secure
TELEGRAM_BOT_TOKEN=8528801413:AAFW7dH8I-soM2m_6cUC_HZGIf_DnDEtbYQ
TELEGRAM_CHAT_ID=-5020049520
EOF

# Проверяем что файл создался
cat .env
```

## 📂 Шаг 5: Создание необходимых директорий

```bash
# Создаём директории для данных и логов
mkdir -p data logs nginx/ssl

# Проверяем структуру
tree -L 2
```

## 🐳 Шаг 6: Сборка и запуск Docker контейнеров

### 6.1. Сборка образов

```bash
cd /opt/neuromagic

# Собираем Docker образы
docker-compose build --no-cache
```

### 6.2. Инициализация базы данных

```bash
# Создаём таблицы в БД
docker-compose run --rm app python -c "
from app.database import init_db
import asyncio
asyncio.run(init_db())
print('✅ База данных инициализирована')
"
```

### 6.3. Создание тестового пользователя

```bash
# Создаём тестового пользователя
docker-compose run --rm app python create_test_user.py
```

### 6.4. Запуск приложения

```bash
# Запуск с Nginx (production)
docker-compose --profile production up -d

# Проверяем статус контейнеров
docker-compose ps

# Смотрим логи
docker-compose logs -f
```

## ✅ Шаг 7: Проверка работы

### 7.1. Проверка на сервере

```bash
# Проверяем что приложение отвечает
curl http://localhost:8000

# Проверяем через Nginx
curl http://localhost
```

### 7.2. Проверка с вашего компьютера

Откройте браузер и перейдите по адресу:
```
http://138.124.15.60
```

Должна открыться главная страница Нейромагии! 🎉

## 🔧 Полезные команды

### Просмотр логов

```bash
# Все логи
docker-compose logs -f

# Только приложения
docker-compose logs -f app

# Только Nginx
docker-compose logs -f nginx
```

### Перезапуск

```bash
# Перезапуск всех контейнеров
docker-compose restart

# Перезапуск только приложения
docker-compose restart app
```

### Остановка

```bash
# Остановка всех контейнеров
docker-compose down

# Остановка с удалением volumes (ОСТОРОЖНО! Удалит БД)
docker-compose down -v
```

### Обновление кода

```bash
cd /opt/neuromagic

# Останавливаем контейнеры
docker-compose down

# Обновляем код из GitHub
git pull origin main

# Пересобираем и запускаем
docker-compose build --no-cache
docker-compose --profile production up -d
```

## 🔐 Шаг 8: Настройка GitHub Actions (опционально)

### 8.1. Настройка GitHub Secrets

Перейдите в репозиторий на GitHub:
```
https://github.com/Starlight19973/magic_ai/settings/secrets/actions
```

Добавьте следующие secrets:

1. **SERVER_HOST**: `138.124.15.60`
2. **SERVER_USER**: `root`
3. **SERVER_PASSWORD**: `PFIwOaXLAo08Wz`

### 8.2. Как работает CI/CD

После настройки secrets, при каждом push в ветку `main`:
1. GitHub Actions подключится к серверу
2. Остановит контейнеры
3. Обновит код (`git pull`)
4. Пересоберёт образы
5. Запустит контейнеры
6. Проверит работоспособность

## 🌐 Шаг 9: Настройка домена (когда купите)

### 9.1. Настройка DNS

В панели управления доменом добавьте A-запись:
```
Тип: A
Имя: @
Значение: 138.124.15.60
TTL: 3600
```

Для поддомена www:
```
Тип: A
Имя: www
Значение: 138.124.15.60
TTL: 3600
```

### 9.2. Настройка SSL сертификата (Let's Encrypt)

```bash
# Установка Certbot
apt install certbot python3-certbot-nginx -y

# Получение сертификата (замените ваш-домен.ru на реальный домен)
certbot certonly --standalone -d ваш-домен.ru -d www.ваш-домен.ru

# Сертификаты будут в:
# /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem
# /etc/letsencrypt/live/ваш-домен.ru/privkey.pem

# Копируем сертификаты в проект
cp /etc/letsencrypt/live/ваш-домен.ru/fullchain.pem /opt/neuromagic/nginx/ssl/
cp /etc/letsencrypt/live/ваш-домен.ru/privkey.pem /opt/neuromagic/nginx/ssl/

# Редактируем nginx.conf и раскомментируем HTTPS блок
nano /opt/neuromagic/nginx/nginx.conf

# Перезапускаем nginx
docker-compose restart nginx
```

### 9.3. Автообновление сертификата

```bash
# Добавляем в cron
crontab -e

# Добавьте строку (обновление каждый день в 3:00)
0 3 * * * certbot renew --quiet && cp /etc/letsencrypt/live/ваш-домен.ru/*.pem /opt/neuromagic/nginx/ssl/ && cd /opt/neuromagic && docker-compose restart nginx
```

## 🐛 Решение проблем

### Контейнеры не запускаются

```bash
# Проверяем логи
docker-compose logs

# Проверяем что порты свободны
netstat -tulpn | grep :80
netstat -tulpn | grep :8000
```

### База данных не создаётся

```bash
# Удаляем старую БД и пересоздаём
rm -f data/neuromagic.db
docker-compose run --rm app python -c "from app.database import init_db; import asyncio; asyncio.run(init_db())"
```

### Nginx не может подключиться к приложению

```bash
# Проверяем что app контейнер запущен
docker-compose ps

# Проверяем сеть
docker network ls
docker network inspect neuromagic_neuromagic-network
```

## 📊 Мониторинг

### Использование ресурсов

```bash
# Использование ресурсов контейнерами
docker stats

# Место на диске
df -h

# Размер образов и контейнеров
docker system df
```

### Очистка

```bash
# Удаление неиспользуемых образов
docker image prune -a

# Удаление неиспользуемых контейнеров
docker container prune

# Полная очистка (ОСТОРОЖНО!)
docker system prune -a --volumes
```

## ✨ Готово!

Ваше приложение развёрнуто и работает! 🎉

- **URL**: http://138.124.15.60
- **Тестовый пользователь**:
  - Username: `testuser`
  - Password: `test123`

---

**Дополнительная помощь**: Если что-то не работает, проверьте логи:
```bash
docker-compose logs -f
```

