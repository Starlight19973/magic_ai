# 📦 Подготовка проекта к GitHub

## ✅ Что уже сделано

1. **Создан `.gitignore`** — исключает из git:
   - `__pycache__/` и `*.pyc` (скомпилированный Python код)
   - `.venv/` (виртуальное окружение)
   - `.env` (секретные данные)
   - `*.db`, `*.sqlite` (база данных)
   - `.vscode/`, `.idea/`, `.cursor/` (настройки IDE)
   - `logs/`, `*.log` (логи)
   - OS файлы (`.DS_Store`, `Thumbs.db`)

2. **Создан `.env.example`** — шаблон для других разработчиков

3. **Обновлен `README.md`** — полная документация проекта

4. **Инициализирован git** — выполнено `git init`

---

## 🚫 Файлы, которые НЕ попадут в GitHub

```
.venv/              # Виртуальное окружение
__pycache__/        # Кэш Python
*.pyc, *.pyo        # Скомпилированные файлы
.env                # Секретные ключи
neuromagic.db       # База данных SQLite
*.log               # Логи
.vscode/            # Настройки VS Code
.idea/              # Настройки PyCharm
.cursor/            # Настройки Cursor
.git/               # Git метаданные
```

---

## 📋 Файлы, которые БУДУТ в GitHub

```
✅ .env.example
✅ .gitignore
✅ Makefile
✅ PROJECT_CONTEXT.md
✅ README.md
✅ TELEGRAM_OAUTH_SETUP.md
✅ app/
✅ create_test_user.py
✅ main.py
✅ pyproject.toml
✅ requirements.txt
✅ static/
✅ templates/
✅ tests/
```

---

## 🚀 Команды для загрузки в GitHub

### 1. Добавить все файлы в staging

```bash
git add .
```

### 2. Создать первый коммит

```bash
git commit -m "Initial commit: Neuromagic platform v1.0

- Quart async web framework
- SQLAlchemy + SQLite database
- User authentication (email/password)
- Telegram OAuth preparation
- Magic design with particles and custom cursor
- Course catalog with filtering
- Responsive UI"
```

### 3. Создать репозиторий на GitHub

Перейдите на https://github.com/new и создайте новый репозиторий:
- **Название:** `neuromagic` (или любое другое)
- **Описание:** "Образовательная платформа для обучения работе с AI"
- **Visibility:** Public или Private
- ❌ **НЕ** создавайте README, .gitignore или LICENSE (они уже есть)

### 4. Подключить удаленный репозиторий

```bash
git remote add origin https://github.com/ВАШ_USERNAME/neuromagic.git
```

### 5. Загрузить код на GitHub

```bash
git branch -M main
git push -u origin main
```

---

## 🔐 Важно: Безопасность

### ⚠️ НИКОГДА не загружайте в GitHub:

- ❌ Файл `.env` с реальными SECRET_KEY
- ❌ База данных `*.db` с реальными пользователями
- ❌ Пароли, API ключи, токены
- ❌ Виртуальное окружение `.venv/`

### ✅ Вместо этого:

- ✅ Используйте `.env.example` с примерами
- ✅ Документируйте настройки в README
- ✅ Храните секреты в GitHub Secrets (для CI/CD)

---

## 📝 Проверка перед push

Выполните проверку:

```bash
# Посмотреть что будет закоммичено
git status

# Посмотреть изменения
git diff --staged

# Проверить что .env НЕ в списке
git ls-files | findstr .env
# Должно показать только: .env.example
```

---

## 🎯 После загрузки

1. Добавьте красивые badges в README (они уже есть)
2. Настройте GitHub Pages (опционально)
3. Добавьте Topics: `python`, `quart`, `ai`, `education`, `sqlite`
4. Напишите Release Notes для v1.0

---

## 🤝 Для других разработчиков

Инструкции для тех, кто будет клонировать репозиторий:

```bash
# 1. Клонировать
git clone https://github.com/USERNAME/neuromagic.git
cd neuromagic

# 2. Создать виртуальное окружение
python -m venv .venv
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/macOS

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Настроить окружение
cp .env.example .env
# Отредактировать .env

# 5. Инициализировать БД
python create_test_user.py

# 6. Запустить
quart --app main:app --debug run
```

---

## 📚 Дополнительно

### Создать `.gitattributes` (опционально)

```bash
echo "* text=auto" > .gitattributes
echo "*.py text eol=lf" >> .gitattributes
echo "*.js text eol=lf" >> .gitattributes
echo "*.css text eol=lf" >> .gitattributes
```

Это обеспечит правильные переносы строк на разных ОС.

---

## ✨ Готово!

Теперь ваш проект готов к публикации на GitHub! 🚀

